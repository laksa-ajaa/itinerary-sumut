{
    "$schema": "https://json.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build",
        "dev": "vite",
        "start": "concurrently -c \"#93c5fd,#c4b5fd,#fb7185\" \"php artisan serve\" \"npm run dev\" \"cd reco_service && source .venv/bin/activate && uvicorn main:app --host 0.0.0.0 --port 8001 --reload\" --names=laravel,vite,reco --kill-others",
        "start:all": "concurrently -c \"#93c5fd,#c4b5fd,#fb7185\" \"php artisan serve\" \"npm run dev\" \"cd reco_service && source .venv/bin/activate && uvicorn main:app --host 0.0.0.0 --port 8001 --reload\" --names=laravel,vite,reco --kill-others"
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.0.0",
        "axios": "^1.11.0",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^2.0.0",
        "tailwindcss": "^4.0.0",
        "vite": "^7.0.7"
    }
}
